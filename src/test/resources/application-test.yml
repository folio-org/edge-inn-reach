server:
  port: 8081
spring:
  application:
    name: edge-inn-reach
  liquibase:
    enabled: false
  jackson:
    default-property-inclusion: NON_EMPTY
feign:
  client:
    config:
      default:
        loggerLevel: full
logging:
  level:
    org:
      springframework:
        cache: OFF
folio:
  tenant:
    validation:
      enabled: false
